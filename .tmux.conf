set -g mouse on

set-window-option -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-pipe "reattach-to-user-namespace pbcopy"

set -g default-shell $SHELL
set -g default-command "tmux rename-window zsh; reattach-to-user-namespace -l $SHELL"
set -g default-terminal "screen-256color"

unbind C-b
set -g prefix C-x

bind-key c new-window -c "#{pane_current_path}"
bind-key v split-window -c "#{pane_current_path}"
bind-key % split-window -h -c "#{pane_current_path}"

set -g history-limit 500000

set -g base-index 1
set-window-option -g pane-base-index 1
set -g renumber-windows on

# TOMORROW NIGHT
# set -g status-bg '#1d1f21'
# set -g status-fg '#cc6666'
# set -g status-left  '                            '
# set -g status-right '#[fg=#f0c674] %a %b %d %G %H:%M:%S %Z'
# set -g status-justify centre

# TOMORROW
set -g status-bg '#ffffff'
set -g status-fg '#cc6666'
set -g status-left  '                            '
set -g status-right '#[fg=#8abeb7] %a %b %d %G %H:%M:%S %Z'
set -g status-justify centre

set -s escape-time 0

# setw -g monitor-activity on

bind-key R source-file ~/.tmux.conf \; display-message "Config reloaded..."
